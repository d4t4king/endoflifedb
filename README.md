# endoflifedb
Utility for downloading the endoflife.date data from the API and converting to various formats.

# TODO
* Strip new lines from data fields (versionCommand)
* test processing dates as integers (easier than string/regex matching
* Add --update
** object comparison?
** create historical updates? OR just update when changes.  (probably the latter)